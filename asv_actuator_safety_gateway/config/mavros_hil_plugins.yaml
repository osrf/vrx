/**:
  ros__parameters:
    # MAVROS's APM defaults deny the HIL plugin. This experiment needs only
    # MAVLink connection state and incoming HIL actuator controls.
    plugin_allowlist:
      - sys_status
      - hil

/**/sys:
  ros__parameters:
    heartbeat_rate: 1.0
    conn_timeout: 3.0
